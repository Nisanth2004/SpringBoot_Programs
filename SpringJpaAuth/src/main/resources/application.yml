spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_jpa_auth
    username: root
    password: Sujith@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect # develop appropriate stmt for the query in mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
